# UIT Runtime

Shared Playwright-free runtime used by UIT Studio. Install `uit-studio` for
the desktop application; this package is published separately so CLI-only
installs do not determine the Studio dependency graph.
